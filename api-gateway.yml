server:
  port: 8089

spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      routes:
        - id: catalog-service
          uri: lb://catalog-service
          predicates:
            - Path=/catalog/**

        - id: order-service
          uri: lb://order-service
          predicates:
            - Path=/orders/**
